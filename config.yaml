# Indexer Configuration
# Specify which apps should be enabled for indexing

enabled_apps:
  - homebase
  # - afterme  # Disabled by default - not live yet
  - trustless

# You can add more apps here as they become available
# Example:
# - myapp
